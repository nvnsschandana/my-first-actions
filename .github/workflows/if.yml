name: if condition
on: push
jobs:
  run-test:
   runs-on: ubuntu-latest
   steps:
     - uses: actions/checkout@v3
     - name: run test
       run: |
         result=$(cat text.txt)
         if [["$result"==*"passed"]]; then
           echo "Test passed"
         else
           echo "Test failed"
     
